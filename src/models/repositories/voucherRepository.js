const _Voucher = require("../voucherModel");
const { getUnSelectData } = require("../../utils/index");
const {
  InternalServerError,
  NotFoundError,
} = require("../../utils/errorHanlder");
class VoucherRepository {
  static async createDiscount(account_id) {}
}
module.exports = VoucherRepository;
